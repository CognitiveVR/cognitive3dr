% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/events.R
\name{c3d_events}
\alias{c3d_events}
\title{Retrieve Cognitive3D event data}
\usage{
c3d_events(
  project_id = NULL,
  exclude_test = TRUE,
  exclude_idle = TRUE,
  start_date = NULL,
  end_date = NULL,
  min_duration = NULL,
  page_limit = 100,
  max_sessions = 500
)
}
\arguments{
\item{project_id}{Integer. Project ID. Uses the default from
\code{c3d_project()} if \code{NULL}.}

\item{exclude_test}{Logical. If \code{TRUE} (default), filter out sessions tagged
as test.}

\item{exclude_idle}{Logical. If \code{TRUE} (default), filter out sessions tagged
as junk/idle.}

\item{start_date}{Date, POSIXct, or \code{"YYYY-MM-DD"} string. Start of date range.
If neither \code{start_date} nor \code{end_date} is provided, defaults to 30 days ago.}

\item{end_date}{Date, POSIXct, or \code{"YYYY-MM-DD"} string. End of date range.
Defaults to now if \code{start_date} is set but \code{end_date} is not.}

\item{min_duration}{Numeric. Minimum session duration in seconds.}

\item{page_limit}{Integer. Number of sessions per API page (default 100).}

\item{max_sessions}{Integer. Hard cap on total sessions to retrieve
events for (default 500).}
}
\value{
A tibble where each row is one event. Session context columns
(\code{session_id}, \code{participant_id}, \code{user_key}, \code{device_id},
\code{session_date}, \code{duration_s}) appear first, followed by core event
columns and dynamic \verb{prop_*} columns.
}
\description{
Queries the Cognitive3D sessions API with event data included, then
unnests and flattens events into a one-row-per-event tibble with
session context attached.
}
\examples{
\dontrun{
c3d_auth()
c3d_project(4460)

# Get events from last 30 days
events <- c3d_events()

# Filter by date range
events <- c3d_events(start_date = "2026-01-01", end_date = "2026-02-01")

# Minimum 60 second sessions, up to 100 sessions
events <- c3d_events(min_duration = 60, max_sessions = 100)
}
}
