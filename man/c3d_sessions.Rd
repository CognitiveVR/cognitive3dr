% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sessions.R
\name{c3d_sessions}
\alias{c3d_sessions}
\title{Retrieve Cognitive3D session data}
\usage{
c3d_sessions(
  project_id = NULL,
  n = 500,
  session_type = c("project", "scene"),
  scene_id = NULL,
  scene_version_id = NULL,
  start_date = NULL,
  end_date = NULL,
  exclude_test = TRUE,
  exclude_idle = TRUE,
  min_duration = NULL,
  compact = TRUE
)
}
\arguments{
\item{project_id}{Integer. Project ID. Uses the default from
\code{c3d_project()} if \code{NULL}.}

\item{n}{Integer. Maximum 500. For \code{session_type = "project"}, the number
of rows returned. For \code{session_type = "scene"}, the cap is applied
\strong{per scene} so every scene is represented—the output may have more
than \code{n} rows because the same session appears once per scene visited.}

\item{session_type}{Character. \code{"project"} (default) returns one row per
session. \code{"scene"} returns one row per session-scene combination, with
metrics scoped to each scene. Scene mode defaults to querying only the
\strong{latest version} of each scene; use \code{scene_version_id} to target a
specific version.}

\item{scene_id}{Character or NULL. UUID of a specific scene to filter to.
Only used when \code{session_type = "scene"}.}

\item{scene_version_id}{Integer or NULL. Specific scene version ID to
filter to. Only used when \code{session_type = "scene"}. When provided,
overrides the default latest-version-per-scene behaviour.}

\item{start_date}{Date, POSIXct, or \code{"YYYY-MM-DD"} string. Start of date range.
If neither \code{start_date} nor \code{end_date} is provided, defaults to 30 days ago.}

\item{end_date}{Date, POSIXct, or \code{"YYYY-MM-DD"} string. End of date range.
Defaults to now if \code{start_date} is set but \code{end_date} is not.}

\item{exclude_test}{Logical. If \code{TRUE} (default), filter out sessions tagged
as test.}

\item{exclude_idle}{Logical. If \code{TRUE} (default), filter out sessions tagged
as junk/idle.}

\item{min_duration}{Numeric. Minimum session duration in seconds. Converted
to milliseconds for the API.}

\item{compact}{Logical. If \code{TRUE} (default), return ~40 key columns. If
\code{FALSE}, return all columns.}
}
\value{
A tibble of session data.
}
\description{
Queries the Cognitive3D sessions API and returns a tidy tibble of session
records with metrics and properties.
}
\details{
\subsection{Column naming}{

Top-level API fields use snake_case (e.g. \code{session_id}, \code{device_id}).
Properties collected by Cognitive3D retain the \code{c3d_} prefix
(e.g. \code{c3d_app_name}, \code{c3d_metrics_fps_score}).
}

\subsection{Scene sessions}{

When \code{session_type = "scene"}, additional columns \code{scene_id},
\code{scene_version_id}, and \code{scene_name} are included. The same \code{session_id}
may appear multiple times (once per scene visited). By default, only the
latest version of each scene is queried to avoid excessive API calls for
projects with many scene versions.
}
}
\examples{
\dontrun{
c3d_auth()
c3d_project(4460)

# Get latest 100 sessions
sessions <- c3d_sessions(n = 100)

# Filter by date range
sessions <- c3d_sessions(start_date = "2026-01-01", end_date = "2026-02-01")

# Include test sessions, minimum 60 second duration
sessions <- c3d_sessions(exclude_test = FALSE, min_duration = 60)

# Get all columns
sessions <- c3d_sessions(compact = FALSE)

# Scene sessions (latest version per scene)
scene_data <- c3d_sessions(session_type = "scene")

# Scene sessions for a specific scene
scene_data <- c3d_sessions(
  session_type = "scene",
  scene_id = "de704574-b03f-424e-be87-4985f85ed2e8"
)
}
}
