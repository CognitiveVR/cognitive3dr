% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objectives.R
\name{c3d_objective_results}
\alias{c3d_objective_results}
\title{Retrieve Cognitive3D objective results}
\usage{
c3d_objective_results(
  project_id = NULL,
  objective_id = NULL,
  objective_version_id = NULL,
  slice_by = c("objective_version", "objective"),
  include_steps = FALSE,
  exclude_test = TRUE,
  exclude_idle = TRUE,
  start_date = NULL,
  end_date = NULL
)
}
\arguments{
\item{project_id}{Integer. Project ID. Uses the default from
\code{c3d_project()} if \code{NULL}.}

\item{objective_id}{Integer or NULL. Filter to a single objective.}

\item{objective_version_id}{Integer or NULL. Filter to a single version.}

\item{slice_by}{Character. \code{"objective_version"} (default) or \code{"objective"}.
Controls result granularity. Version mode includes a \code{version_number}
column matching the dashboard's "Version N" label.}

\item{include_steps}{Logical. If \code{TRUE}, also fetch step-level results
for each objective version. Returns a list of two tibbles instead of
a single tibble.}

\item{exclude_test}{Logical. If \code{TRUE} (default), filter out sessions
tagged as test.}

\item{exclude_idle}{Logical. If \code{TRUE} (default), filter out sessions
tagged as junk/idle.}

\item{start_date}{Date, POSIXct, or \code{"YYYY-MM-DD"} string. Start of date range.
If neither \code{start_date} nor \code{end_date} is provided, defaults to 30 days ago.}

\item{end_date}{Date, POSIXct, or \code{"YYYY-MM-DD"} string. End of date range.
Defaults to now if \code{start_date} is set but \code{end_date} is not.}
}
\value{
A tibble. If \code{include_steps = FALSE}, objective-level results. If
\code{include_steps = TRUE}, step-level results.
}
\description{
Queries the Cognitive3D objectives API and returns a tidy tibble of
objective success/failure results, enriched with objective names from
project metadata. Optionally includes step-level detail.
}
\details{
This function coordinates three API endpoints:
\enumerate{
\item GET objectives metadata (names, versions, step definitions)
\item POST objective result queries (success/failure counts)
\item POST objective step result queries (per-step detail, if requested)
}
}
\examples{
\dontrun{
c3d_auth()
c3d_project(4460)

# Get objective results (per version by default)
results <- c3d_objective_results()

# Get step-level detail
steps <- c3d_objective_results(include_steps = TRUE)

# Filter to a specific objective
results <- c3d_objective_results(objective_id = 268)
}
}
